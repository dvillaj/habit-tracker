[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "==2.3.0"
gunicorn = "==23.0.0"
pyyaml = ">=6.0.2"
python-dotenv = ">=1.0.1"

[dev-packages]

[requires]
